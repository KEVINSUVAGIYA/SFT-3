function NoOfDigit(){
    var number = document.getElementById("number").value;
    // var count = number.length;
    // for(var i=0; i<number.length; i++){
    //     if (String(number).charAt(i)!=0){
    //         count -= i;
    //         break;
    //     }
    // }
    alert("number of Digits in" + number + ":" + number.length);
}